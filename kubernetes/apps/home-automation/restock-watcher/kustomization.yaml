apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: home-automation

resources:
  - secret.sops.yaml
  - cronjob.yaml

configMapGenerator:
  - name: restock-watcher-script
    files:
      - checker.ts
