apiVersion: v1
kind: Service
metadata:
  name: claude-db
  labels:
    app: claude-db

spec:
  type: ClusterIP
  selector:
    app: claude-db
  ports:
    - name: http
      port: 3000
      targetPort: 3000
