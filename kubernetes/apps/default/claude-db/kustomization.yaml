---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default

resources:
  - ./secret.sops.yaml
  - ./deployment.yaml
  - ./service.yaml
  - ./ingress.yaml
  - ./ingress-ui.yaml
  - ./ingress-outpost.yaml
  - ./service-outpost.yaml
  - ./networkpolicy.yaml
  - ./backup-cronjob.yaml

configMapGenerator:
  - name: claude-db-backup-script
    files:
      - backup.ts
